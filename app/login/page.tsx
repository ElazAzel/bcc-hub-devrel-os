"use client";

import { useState } from "react";
import { ArrowRight, LockKeyhole, WifiOff } from "lucide-react";
import { isCloudMode, signIn } from "@/lib/data";
import { Button, Field, Input } from "@/components/ui";

export default function LoginPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setLoading(true); setError(""); try { const result = await signIn(email, password); if (result.error) throw result.error; window.location.href = "/"; } catch (err) { setError(err instanceof Error ? err.message : "Не удалось войти"); } finally { setLoading(false); } } return <main className="flex min-h-screen items-center justify-center bg-[#FBF9FF] px-4 py-8"><div className="grid w-full max-w-4xl overflow-hidden rounded-3xl border border-bcc-border bg-white shadow-soft md:grid-cols-[.95fr_1.05fr]"><section className="relative hidden overflow-hidden bg-bcc-deep p-8 text-white md:block lg:p-10"><div className="absolute -right-20 -top-16 h-64 w-64 rounded-full bg-bcc-violet blur-2xl" /><div className="absolute -bottom-24 -left-20 h-72 w-72 rounded-full bg-bcc-lilac/40 blur-3xl" /><div className="relative z-10 flex h-full flex-col justify-between"><div><div className="flex items-center gap-2.5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-white text-sm font-bold text-bcc-deep">B</span><span className="font-semibold">BCC HUB <span className="font-normal text-white/60">/ DevRel OS</span></span></div><div className="mt-24"><div className="eyebrow text-white/60">Personal operating system</div><h1 className="mt-4 text-4xl font-semibold tracking-[-0.05em]">Память о работе, которая помогает двигаться дальше.</h1><p className="mt-5 max-w-sm text-sm leading-6 text-white/70">Контакты, события, задачи и решения — в контексте друг друга.</p></div></div><div className="font-mono text-xs text-white/50">{`{ context: true, next_action: "visible" }`}</div></div></section><section className="p-6 sm:p-8 lg:p-10"><div className="md:hidden"><div className="flex items-center gap-2.5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-bcc-violet text-sm font-bold text-white">B</span><span className="font-semibold">BCC HUB <span className="font-normal text-[#8A8A90]">/ DevRel OS</span></span></div></div><div className="mt-10 md:mt-0"><div className="eyebrow">Private workspace</div><h2 className="mt-2 text-3xl font-semibold tracking-[-0.04em]">Войти в DevRel OS</h2><p className="mt-3 text-sm leading-6 text-[#74747C]">Данные синхронизируются через Supabase. Публичной регистрации внутри приложения нет.</p>{!isCloudMode && <div className="mt-5 flex gap-2 rounded-2xl bg-bcc-cyan/50 p-3 text-sm text-[#3E6B6E]"><WifiOff size={17} className="mt-0.5 shrink-0" /><span>Local development mode: Supabase env не задан, вход будет пропущен.</span></div>}<form onSubmit={submit} className="mt-8 space-y-4"><Field label="Email"><Input type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@company.com" /></Field><Field label="Пароль"><Input type="password" required={isCloudMode} value={password} onChange={(event) => setPassword(event.target.value)} placeholder={isCloudMode ? "••••••••" : "Можно оставить пустым"} /></Field>{error && <p className="rounded-xl bg-[#FDECEC] px-3 py-2 text-sm text-[#AF3030]">{error}</p>}<Button type="submit" variant="brand" className="w-full" disabled={loading}><LockKeyhole size={16} />{loading ? "Проверяем…" : isCloudMode ? "Войти" : "Продолжить в local mode"}<ArrowRight size={16} /></Button></form><p className="mt-6 text-xs leading-5 text-[#8A8A90]">Для production создай пользователя в Supabase Auth и добавь `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.</p></div></section></div></main>; }
